import React from 'react';
import './MainBody.css';
import CaptureDeadline from '../CaptureDeadline/CaptureDeadline'

function MainBody() {
    return (
        <main className="App-MainBody">
          <p>wait for it...</p>
          <CaptureDeadline />
        </main>
    );
  }
  
  export default MainBody;
  